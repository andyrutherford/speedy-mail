import styled from 'styled-components';

export const InboxWrapper = styled.div`
  max-width: 1200px;
  margin: auto;
  h1,
  p {
    text-align: center;
  }

  .inbox-status {
    margin: 40px auto;
  }
`;
